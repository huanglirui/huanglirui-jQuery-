//jQuery的extend方法。实现对象的拷贝继承。支持浅拷贝和深拷贝
//extend即使实例方法也是工具方法
jQuery.extend = jQuery.fn.extend = function () {
  
  //定义一些变量
  
  if () {}   //看是不是深拷贝情况
  
  if () {}   //看参数正确不
  
  if () {}   //看是不是插件情况
  
  for () {   //可能有多个对象情况
    
    if () {}  //防止循环引用
    
    if () {}   //深拷贝
    else if () {}   //浅拷贝
    
  }
  
};